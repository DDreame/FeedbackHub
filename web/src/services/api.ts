/* ═══════════════════════════════════════════
   FeedbackHub API Service
   Re-exports from modular api/ directory.
   All existing imports continue to work unchanged.
   New code should import from './api/<module>' directly.
   =========================================== */

export * from './api/index';
